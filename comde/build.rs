use std::env;
use std::fs::File;
use std::io::{BufWriter, Write};
use std::path::Path;

// use comde::phf::compressed_phf_map;
// use comde::xz::XzPhfMap;

fn main() {
    // let path = Path::new(&env::var("OUT_DIR").unwrap()).join("codegen.rs");
    // let mut file = BufWriter::new(File::create(&path).unwrap());

    // write!(&mut file, "static TEST_XZ: XzPhfMap<&'static str, String> = ").unwrap();
    // phf_codegen::Map::new()
    //     .entry("boop", "Keyword::Loop")
    //     .build(&mut file)
    //     .unwrap();
    // write!(&mut file, ";\n").unwrap();
}
